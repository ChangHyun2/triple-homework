import styled from '@emotion/styled'

export const StyledCounter = styled.span`
  position: relative;

  .skeleton {
    visibility: hidden;
  }

  .value {
    right: 0;
    position: absolute;
  }
`
